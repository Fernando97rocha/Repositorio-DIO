// usando map
const array = [1, 2, 3, 4];

array.map((item) => item * 2); //retorno: [2, 4, 6, 8]


// usando forEach

const array2 = [1, 2, 3, 4];

array.forEach((item) => item * 2); //retorno: undefined

